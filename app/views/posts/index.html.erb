<!-- Posts#index -->
<!-- Find me in app/views/posts/index.html.erb -->
<div>
<div id="showpost">
	<% @posts.each do |p| %>
		<%=	link_to p.body, post_path(p.id) %>
	<%= p.user.fname%></br>
	<% end %>
</div>
</div>
 
 <%= link_to 'Create new Post', new_post_path %>

    